node_modules
dist
frontend/node_modules
backend/node_modules
# Root-level .env (if created)
/.env
# Backend specific environment file
backend/.env
# Example file is safe to commit; real .env should stay local
# output
backend/uploads
.DS_Store