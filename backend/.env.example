# Backend environment configuration
# Copy this file to .env and adjust values. NEVER commit your real secrets.

# Port the Express server listens on
PORT=4000

# Directory for uploaded workbooks (will be created if missing)
UPLOAD_DIR=./uploads

# Gemini (Generative AI) configuration
# Choose ONE auth method: API key or access token (Bearer).
GEMINI_API_URL=https://generativelanguage.googleapis.com/v1beta/models/gemini-pro:generateContent
# If using API key style:
GEMINI_API_KEY=replace_with_your_api_key
# If using Bearer token style:
# GEMINI_ACCESS_TOKEN=replace_with_your_access_token

# Optional OpenAI compatibility (if you later add that pathway)
# OPENAI_API_KEY=replace_with_openai_key

# Increase to safely cap max group columns pulled from training_consumption
MAX_GROUP_COLUMNS=160

# Logging verbosity (info|debug|warn)
LOG_LEVEL=info
